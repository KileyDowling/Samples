@model SGCorpHR.Models.Response<List<SGCorpHR.Models.PolicyDocument>>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>View Policy Documents</h2>

<div>




    @if (Model.Data != null)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>Category</th>
                    <th>File Path</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var policyDoc in Model.Data)
                {
                    <tr>
                        <td>@policyDoc.Name</td>
                        <td>@policyDoc.Category</td>
                        <td>
                            @policyDoc.FilePath
                            @*<form action="/Home/DeleteSuggestion" method="Post">
                                <input type="hidden" name="SuggestionID" id="SuggestionID" value="@suggestion.SuggestionID" />
                                <button>Delete</button>
                            </form>*@
                        </td>

                    </tr>
                }
            </tbody>

        </table>
    }
    else
    {
        { <p>@Model.Message</p>}
    }
</div>

